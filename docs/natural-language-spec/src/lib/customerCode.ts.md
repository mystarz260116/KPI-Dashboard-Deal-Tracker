# src/lib/customerCode.ts

## 対応元ファイル

`src/lib/customerCode.ts`

## 役割

フロントエンドまたは共有処理で使うユーティリティです。

## 主な仕様

- 画面やAPI呼び出しで重複しやすい処理をまとめます。
- 認証、日付、顧客コード、MFA、性能ログなどの横断的な関心ごとを扱います。

## コードから読み取れる手がかり

- 主な公開要素: normalizeCustomerCode、normalizeCustomerName

## 運用メモ

- この説明は現在のファイル構造に合わせた自然言語版です。実装変更時は対応する説明も更新してください。
- 種別: テキストとして読めるファイル。
- TypeScript/Reactの型やAPI契約が変わる場合、呼び出し元とサーバーハンドラーの両方を確認してください。
